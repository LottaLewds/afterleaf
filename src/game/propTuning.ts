/** Scale limits for user-placed model props. */
export const DEFAULT_MODEL_SCALE = 1;
export const MIN_MODEL_SCALE = 0.1;
export const MAX_MODEL_SCALE = 10;

/** Held-prop projection distance limits while carrying a prop. */
export const PROP_MIN_PROJECTION_DISTANCE = 0.9;
export const PROP_MAX_PROJECTION_DISTANCE = 6;
